---
description: Verify Build Tools Path
---

1. Check if `link.exe` is in the path.
// turbo
2. Check if `cl.exe` is in the path.
// turbo
3. Check environment variables for Visual Studio installation.
